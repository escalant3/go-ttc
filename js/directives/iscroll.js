angular.module('GoTTC')
.directive('iscroll', [
    function() {
      return {
        link: function(scope, elem) {
          // Create the iScroll
         // new iScroll('wrapper');
        }
      };
    }
]);
